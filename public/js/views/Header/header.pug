- var searchUrl = '#'
case role
    when 'seeker'
        - var searchUrl = '/search/vacancy'
    when 'employer'
        - var searchUrl = '/search/resume'
    default
        - var searchUrl = '/search/vacancy'

div.header__logo
    a(href='/')
        img(src='/img/ibat.jpg', alt='logo')

if role === 'seeker'
    nav.header__menu
        a.link.header__link.header__menu-link(href='#') Мои резюме
        a.link.header__link.header__menu-link(href='/my_responds') Отклики

div.header__fill

div.header__search
    div.header__search-button
        a.icon.header__search-icon(href=`${searchUrl}`)
        a.header__link(href=`${searchUrl}`) Поиск

if role
    case role
        when 'seeker'
            a.button.header__button(href='/createresume') Создать резюме

            div.header__favorites
                a.icon.header__favorites-icon(href='/favorite_vacancies')

        when 'employer'
            a.button.header__button(href='/createvacancy') Разместить вакансию

            div.header__favorites
                a.icon.header__favorites-icon(href='/favorite_resumes')

    div.header__profile
        a.icon.header__profile-icon(href='/profile')

    input.button.header__button(type='button', name='signOut', value='Выйти')

else
    a.button.header__button(href='/signin') Войти
